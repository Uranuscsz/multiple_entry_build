{
  "name": "multiple_entry_build",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev-nested": "vite --mode dev-nested",
    "build": "vue-tsc && vite build --mode build",
    "build-index": "vue-tsc && vite build",
    "build-nested": "vue-tsc && vite build --mode build-nested",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.2.45"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.0.0",
    "typescript": "^4.9.3",
    "vite": "^4.0.0",
    "vite-plugin-html": "^3.2.0",
    "vue-tsc": "^1.0.11"
  }
}
