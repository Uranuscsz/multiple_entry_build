{
  "name": "multiple_entry_build",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --mode dev",
    "dev-nested": "vite --mode nested",
    "build": "vue-tsc && vite build --mode build",
    "build-s-nested": "vue-tsc && vite build --mode nested",
    "build-nested": "vue-tsc && vite build --mode build-nested"
  },
  "dependencies": {
    "vue": "^3.2.45"
  },
  "devDependencies": {
    "@types/node": "^18.11.17",
    "@vitejs/plugin-vue": "^4.0.0",
    "typescript": "^4.9.3",
    "vite": "^4.0.0",
    "vue-tsc": "^1.0.11"
  }
}
